class Card{

  constructor(x, y){
    this.x = x;
    this.y = y;
    this.width = 100;
  }

//Using canvas to draw rectangle

  drawFaceDown () {
      // fill(214, 247, 202);
      // stroke(2);
      // strokeWeight(2);
      // rect(this.x, this.y, this.width, this.width, 10);
      // image(getImage("../../app/images/TIYlogo.jpg"), this.x, this.y, this.width, this.width);
  };



}

export {Card};
